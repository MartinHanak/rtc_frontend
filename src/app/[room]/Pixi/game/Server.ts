// updates old game state given user inputs
export class Server {

    private tickRate: number = 10; // tickRate per second update

    private currentSimulationTime;

    private playerCommandsBuffer;

    private previousGameState; // includes previous time
}